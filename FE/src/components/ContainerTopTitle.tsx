import styled from 'styled-components';
import Container from './Container';

const ContainerTopTitle = styled(Container)`
    font-size: 18px;
    font-weight: 700;
`;
export default ContainerTopTitle;
