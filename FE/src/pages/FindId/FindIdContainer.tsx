import styled from 'styled-components';
import Container from '../../components/Container';

const FindIdContainer = styled(Container)`
    flex-direction: column;
    width: 100vw;
    height: 100vh;
    min-width: 750px;
`;

export default FindIdContainer;
