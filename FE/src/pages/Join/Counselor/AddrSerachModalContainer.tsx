import styled from 'styled-components';
import Container from '../../../components/Container';

const AddrSearchModalContainer = styled(Container)`
    width: 100vw;
    height: 100vh;
    background-color: rgba(0, 0, 0, 0.2);
    position: absolute;
`;

export default AddrSearchModalContainer;
