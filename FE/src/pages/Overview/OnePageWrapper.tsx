import styled from 'styled-components';
import Wrapper from '../../components/Wrapper';

const OnePageWrapper = styled(Wrapper)`
    width: 100vw;
    min-width: 750px;
    height: 100vh;
    flex-direction: column;
`;

export default OnePageWrapper;
